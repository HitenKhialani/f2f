MASTER IMPLEMENTATION PROMPT

I have added a folder named Stitch inside the repository.
Inside Stitch, there are three separate folders containing:

Landing Page – Desktop Design

Landing Page – Mobile Design

Mobile Responsive Layouts for all portals

Your task is to implement this properly with the following strict rules:

🔹 TASK 1: Fix Mobile Responsiveness (Mobile-First Architecture)

The website must follow a mobile-first approach.

The existing desktop layout must NOT be changed in any way.

The current desktop UI is correct and should remain visually identical.

The mobile responsiveness is currently broken — this must be rebuilt properly.

Required Implementation Rules:

Use a mobile-first CSS structure.

Base styles must target mobile screens first.

Use media queries like:

@media (min-width: 768px)

to progressively enhance for tablet and desktop.

If required, use separate layout wrappers for mobile and desktop.

It is allowed to conditionally render different layouts using:

CSS media queries

Tailwind responsive utilities

or React conditional layout rendering

Critical Requirement:

Desktop and mobile must behave as two independent layout systems.

Meaning:

When opened on desktop → it should render the desktop layout exactly as designed.

When opened on mobile → it should render the mobile layout exactly as designed.

No broken stacking.

No crushed spacing.

No half-responsive compromise.

No shrinking desktop layout into mobile.

Take layout references from:

/Stitch/

Carefully analyze spacing, alignment, typography scale, component placement, and hierarchy.

If some pages or components are missing in the Stitch folder:

Infer layout logic from existing design system patterns.

Maintain visual consistency.

No shortcuts. No assumptions.

🔹 TASK 2: Replace Landing Page Completely

Replace the existing landing page with the design provided in the Stitch folder.

Requirements:

Follow the exact layout structure.

Match typography, spacing, sections, hierarchy.

Maintain theme consistency across entire project.

Ensure full mobile compatibility (mobile-first).

Do NOT affect other portal pages.

🔹 TASK 3: Integrate Lenis for Smooth Animations

Install and use Lenis (React-compatible implementation) for smooth scrolling animations.

Implementation rules:

Smooth scrolling must feel premium and subtle.

Do NOT overuse animations.

Use it for:

Scroll transitions

Section reveals (optional but elegant)

Ensure performance is maintained.

Must work smoothly on both mobile and desktop.

Lenis must not break:

Routing

Scroll restoration

Page transitions

🔹 STRUCTURE RULES

Clean folder structure.

Reusable components.

No duplicated code.

No inline styling unless absolutely necessary.

Keep layout logic separated from UI logic.

Do not touch backend.

Do not change APIs.

Do not modify business logic.

🔹 FINAL QUALITY CHECK

Before completion, verify:

Desktop matches original desktop design pixel-to-pixel.

Mobile matches Stitch mobile design exactly.

No horizontal scroll.

No overflow issues.

Proper spacing at all breakpoints.

Navigation works on both views.

Performance is stable.

This is a mobile-first production-grade refactor.

Do not rush.
Do not guess.
Follow the Stitch layout references carefully.